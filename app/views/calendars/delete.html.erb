<div class="hero-unit">
<h1>Usuń Kalendarz</h1>

<%= form_for(:calendar, :url => {:action => 'destroy', :id => @calendar.id}) do |pole| %>

<p >Czy napewno chcesz usunąć Kalendarz: <p class ="text-warning"><%= @calendar.name %> ?<p/></p>


<div>
	<%= submit_tag("Usuń",  {:class => "btn btn-mini btn-warning"}) %>
	<%= link_to("powrót", {:action=> 'index'}, {:class => "btn btn-mini btn-primary"}) %>
</div>

<% end %>

</div>